enum VideoState {
  initial,
  loading,
  playing,
  paused,
  error,
  disposed,
}

enum VideoVisibilityState {
  visible,
  hidden,
  background,
}

enum VideoLoadState {
  initial,
  loading,
  loaded,
  loadingMore,
  error,
  noMore,
}
