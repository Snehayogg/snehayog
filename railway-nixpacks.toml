[build]
builder = "NIXPACKS"

[deploy]
startCommand = "cd snehayog/backend && npm run setup:ffmpeg && npm start"
healthcheckPath = "/health"
healthcheckTimeout = 100
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10

[env]
NODE_ENV = "production"
