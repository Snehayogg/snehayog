[build]
builder = "DOCKERFILE"
buildCommand = "cd snehayog/backend && npm install && npm run setup:ffmpeg"

[deploy]
healthcheckPath = "/health"
healthcheckTimeout = 100
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10

[env]
NODE_ENV = "production"